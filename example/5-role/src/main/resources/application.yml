server:
  port: 12035
logging:
  level:
    org:
      springframework:
        security: DEBUG
      hibernate:
        SQL: debug
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
